name: AI Docs

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  update-docs:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install

      - name: Install Codex CLI
        run: npm install -g @openai/codex

      - name: Run AI docs update
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }} # Ensure the API key is available for Codex CLI
        run: |
          codex --model gpt-4o exec "
          Read .ai/prompts/update-docs.md
          Analyze git diff against main branch
          Update AGENTS.md, docs, and skills accordingly
          "

      - name: Commit changes
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git diff --quiet && echo "No changes" || git commit -m "AI update docs"
          git push

      - name: Generate PR summary
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          SUMMARY=$(codex --model gpt-4o exec "Analyze git diff and generate concise PR summary")
          gh pr edit ${{ github.event.pull_request.number }} \
            --body "$SUMMARY"